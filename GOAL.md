# mem 项目目标

> **mem 是面向 AI Agent 的可迁移网盘。**
>
> 它让 Agent 的工作资产、长期记忆和任务状态归用户所有，并能在不同
> Agent、不同会话和不同电脑之间无痛接续；同时让用户像使用普通网盘一样，
> 看见、整理、搜索、导出和删除自己的全部内容。

## 1. 我们要解决的问题

今天，用户在 Claude Code、Codex、Cursor 等 Agent 中积累的上下文往往被锁在
某个工具、某次会话或某台电脑里。切换 Agent 或设备时，用户需要重新解释背景、
寻找文件、复制规则，并人工说明“上次做到哪里”。

mem 要把这些本应属于用户的数据从 Agent 宿主中解耦出来，形成一个独立、持久、
可检查、可迁移的个人工作空间。

理想体验是：

- **切换 Agent**：Claude Code 写入交接状态后，Codex 能立即获得同一任务的目标、
  当前进度、关键决定、未解决问题、相关文件和出处，并继续工作。
- **切换电脑**：用户在新设备登录并选择 workspace 后，可以恢复所需的工作资产、
  Agent 记忆和经过用户授权的偏好配置，不必手工搬运上下文。
- **回到人类视角**：用户能在 Web UI 中像浏览网盘一样查看原始文件、Agent 写入的
  记忆、任务交接和版本关系，并决定哪些内容保留、纠正、导出或遗忘。

这里的“无痛迁移”不是复制某个 Agent 的私有黑盒，而是用开放、稳定的数据契约保存
可复用状态，再由不同 Agent 适配器读写同一份用户数据。

## 2. 一个产品，两种入口

mem 只有一份数据和能力内核，但面向两类使用者：

| 入口 | 核心价值 |
|---|---|
| **Agent 入口：API / MCP / CLI** | 写入任务状态与产物，召回带出处的上下文，完成跨 Agent、跨会话和跨设备接续 |
| **人的入口：Web UI** | 像网盘一样浏览、搜索和管理原件、记忆、交接记录、权限与生命周期 |

因此，“Agent Memory”和“AI 网盘”不是两个产品：前者是机器使用面，后者是人的
可见、可信和可控界面。

自然语言搜图、搜文件和跨模态关联是这个网盘的核心能力，也是首发杀手场景。它们
让人和 Agent 能从迁移后的海量资产中真正找回所需内容；迁移解决“带得走、接得上”，
智能检索解决“找得到、用得准”，两者不能互相替代。

## 3. 必须保存什么

一个可迁移 workspace 至少包含：

1. **原始资产**：文件、图片、音频、代码产物及稳定内容哈希。
2. **任务状态**：目标、当前进度、已完成事项、下一步和未解决问题。
3. **决定与依据**：做了什么选择、为什么、引用了哪些来源。
4. **长期记忆**：用户确认的偏好、约束、纠正记录及其适用范围。
5. **关系与出处**：Agent、会话、任务、时间、路径、版本和 source locator。
6. **可移植清单**：开放、版本化的 manifest，用于校验、导出、导入和恢复。

密钥、登录凭证等敏感信息默认不进入可迁移包；如未来支持，必须单独加密、授权和
审计。

## 4. 北极星验收场景

### A. Claude Code → Codex

1. Claude Code 在任务结束或切换前写入标准 handoff。
2. Codex 连接同一 workspace，读取 handoff 和相关证据。
3. Codex 能正确说出当前目标、最近决定、工作树/产物状态、阻塞项和下一步。
4. 用户无需重新复述项目背景即可继续。

### B. 旧电脑 → 新电脑

1. 用户在新电脑安装客户端并登录。
2. 选择 workspace 后，客户端发现可恢复的资产、记忆和配置清单。
3. 恢复过程可校验、可中断重试，并明确展示未恢复或冲突的项目。
4. Agent 能从最近一次有效状态继续任务。

### C. 用户可视化管理

1. 用户能在 Web UI 同时看到文件、结构化记忆和任务交接。
2. 每条 Agent 记忆都能追溯到写入者、时间、任务和来源。
3. 用户可以搜索、预览、纠正、导出、归档或遗忘。
4. UI 与 API / MCP / CLI 使用同一语义，不存在不可见的“第二份记忆”。

## 5. 现状与愿景的偏差

最初评估结论是：**底层方向正确，但产品重心偏向“搜索网盘”，可迁移与用户控制
明显落后。** 本轮已经把这项偏移纠正到可验收的 MVP，不代表全部愿景已经完成。

| 维度 | 当前可验证状态 | 判断 |
|---|---|---|
| 统一存储与网盘界面 | 文件/文件夹、上传下载、详情、自然语言搜索与对象存储共用同一数据面 | **对齐** |
| Agent 开放入口 | API、CLI、MCP 共用服务端契约，并受 workspace、scope 与 path 约束 | **对齐** |
| 跨 Agent 记忆 | `remember/context`、出处、幂等、反馈、归档、恢复和遗忘控制闭环已实现 | **MVP 对齐** |
| 标准任务交接 | `mem.handoff` v1、不可变 checkpoint、CAS、resume、哈希引用和缺失项报告已实现 | **MVP 对齐** |
| Claude Code / Codex 迁移 | 以 Claude Code / Codex 身份隔离的两个独立 Token 已通过真实 HTTP/PostgreSQL 写入与只读恢复验收；两端有同一个标准 MCP adapter 的接入说明 | **MVP 对齐；仍需真实宿主进程的发布级 smoke** |
| 跨设备恢复 | 同一部署可登录 workspace 继续使用；跨部署可导出 `.membundle` 并 `fresh` 导入空 workspace，带完整性校验、幂等 ledger 和结构化冲突 | **部分对齐：尚无 merge、增量同步和断点上传** |
| 人类可视化 | Web 已覆盖 Drive、Search、Tasks、checkpoint/Resume、Memories 生命周期与 Workspace Transfer | **MVP 对齐：尚缺 correction/supersede 与完整审计历史** |
| 数据可移植性 | workspace bundle v1 有开放 schema、七类索引、payload/blob checksum、依赖校验和真实数据库 round-trip | **MVP 对齐：当前服务只支持 fresh restore** |
| 自然语言搜图 | 原始图片字节→512 维视觉向量→文本塔查询→可回原件的链路已实现；真实英文固定集通过 | **链路对齐、质量未完全对齐：默认模型中文固定集未通过** |

因此，项目现在不再只是“可视化的 Agent Memory / AI 搜索网盘”，而是已经具备
可迁移 Agent 网盘的最小闭环。自然语言搜图从来不是偏离；偏移只会发生在智能检索
成为唯一主线，而跨 Agent 接续、跨设备恢复和用户控制长期缺席时。后续仍应并行
建设“带得走、接得上”和“找得到、可验证”两条能力线，并让它们共享同一份原件、
出处与权限语义。

上述场景对应的可重复验收清单见
[docs/acceptance/NORTH_STAR.md](docs/acceptance/NORTH_STAR.md)。

## 6. 近期优先级

### P0 — 跑通跨 Agent 接续

- [x] 定义版本化 `handoff` / `checkpoint` / `resume` 数据契约。
- [x] 将完整任务状态放进不可变 checkpoint；自由文本记忆继续承担独立 occurrence。
- [x] 提供 Claude Code 与 Codex 共用标准 MCP adapter 的接入说明。
- [x] 建立 “Claude Code 写入 → Codex 恢复” 的双 Token 端到端验收测试。

### P1 — 跑通可移植与跨设备恢复

- [x] 定义包含 manifest、内容哈希、schema 版本和依赖关系的 `.membundle` v1。
- [x] 实现 API / CLI / Web 的 workspace export 与空目标 `fresh` import。
- [x] 实现导入前校验、幂等 ledger、冲突明细、失败补偿和导入后重新索引。
- [ ] 实现 `merge_conservative`、增量包、断点上传与完整本地同步盘体验。

### P2 — 让全部 Agent 数据可见可控

- [x] 在 Web UI 增加记忆、任务交接、来源、版本、Resume 与迁移视图。
- [x] 提供反馈、置顶、归档、恢复、确认遗忘和 workspace 导出/导入。
- [ ] 提供不可变 correction/supersede、导入历史和更完整的权限管理界面。

### 持续主线 — 自然语言搜图与多模态召回

- 保留“人物 + 时间 + 地点 + 事件”的自然语言搜图作为杀手场景。
- 持续增强图片 caption、视觉向量、实体、时间和关系的混合召回。
- 每个结果都能打开原件并解释命中原因；不能以不可验证的生成答案替代检索结果。
- 建立中英文固定图片集评测；默认视觉模型未通过中文查询前，不把中文示例当作
  已达标能力。
- 同时衡量检索质量与迁移/恢复成功率，任何一方都不能掩盖另一方的缺口。

## 7. 产品决策原则

新功能至少应明显改善以下一项，否则不进入近期主线：

1. 跨 Agent / 跨会话接续成功率。
2. 跨设备迁移与恢复的完整性、可靠性或可理解性。
3. 用户对存储内容的可见性、所有权与控制力。
4. Agent 召回真实工作状态时的准确性、出处与安全边界。
5. 自然语言搜图、搜文件和多模态关联的准确率与可验证性。

mem 不做 Agent runtime、聊天产品或模型宿主，也不依赖某一家 Agent 的私有格式作为
内部真相。协议可以适配宿主，用户数据不能被宿主绑定。
