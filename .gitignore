# === OS ===
.DS_Store
Thumbs.db
*.swp
*~

# === Editors ===
.idea/
.vscode/
*.iml

# === Env / Secrets ===
.env
.env.local
.env.*.local
*.key
*.pem
secrets/

# === Go (server/) ===
# repo-root build output from `make build`
/bin/
server/bin/
server/dist/
# stray `go build` output dropped directly in server/ (no path flag)
server/memd
server/mem
server/mem-mcp
server/coverage.out
server/coverage.html
server/*.test
server/.air.toml

# === Python (worker/) ===
worker/__pycache__/
worker/**/__pycache__/
worker/*.pyc
worker/.venv/
worker/venv/
worker/dist/
worker/build/
worker/*.egg-info/
worker/.pytest_cache/
worker/.ruff_cache/
worker/.mypy_cache/

# === Node / Web (web/) ===
web/node_modules/
web/dist/
web/build/
web/.next/
web/out/
web/coverage/
web/.turbo/

# === Generated proto ===
# Generated Go and Python protobuf stubs are checked in so a fresh clone builds.
# Regenerate them with `make proto`; CI verifies that the checked-in output is current.

# === Local data / scratch ===
# Root-anchored so we don't accidentally swallow Go packages named "storage".
/data/
/storage/
.scratch/
*.log

# === Bare-metal dev stack (scripts/dev_up.sh) ===
# Local runtime: postgres cluster, minio data, downloaded binaries, logs, pids.
/.dev/

# === Docker ===
.docker-data/

# === Misc ===
node_modules/
*.tsbuildinfo

# === Agent worktrees (Claude Code worktree isolation) ===
.claude/

# === Local dev overrides ===
docker-compose.override.yml

# Legacy local provider credential script; never commit its contents.
scripts/cloud_env.sh
