{
  "mcp_servers": {
    "mem": {
      "command": "${MEM_MCP_COMMAND}",
      "args": [],
      "env": {
        "MEM_SERVER": "${MEM_SERVER}",
        "MEM_TOKEN": "${MEM_TOKEN}",
        "MEM_WORKSPACE": "${MEM_WORKSPACE}"
      },
      "enabled": true,
      "timeout": 300
    }
  }
}
